services:
  openterms:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FREE_MODE=true
    volumes:
      - openterms-data:/app/data
    restart: unless-stopped

volumes:
  openterms-data:
